>> Declaring variables and applaying mathematical concepts

> #include <stdio.h>
> int main(){
  int a;
  int b; 
  
  // For the fisrt problem we will make (a + b)
  // Let's give values to variable 'a' and 'b'
  
  a = 5;
  b = 3;
  
  // Now, to see the result we must use the function 'printf()'
  // In C Language, we must say what type is the program going to print so %d or %i are used to Integers (int) variables
  
  printf("(%d | %d | %d | %d)",(a+b),(a*b),(a/b),(a-b)); 
  
  
  
}



